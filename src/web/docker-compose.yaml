services:
  nextjs:
    platform: linux/amd64
    build:
      context: .
      dockerfile: Dockerfile
    image: talk-to-bi-app

    ports:
      - "8081:3000"
    env_file:
      - .env.local

    environment:
      NODE_ENV: production